import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "guestbook · arjita" };

export default function GuestbookPage() {
  return (
    <PlaceholderPage
      catNo="006"
      title="guestbook"
      note="the ledger is at the bookbinder's. soon your entry will be filed here permanently, folder tab and all."
    />
  );
}
