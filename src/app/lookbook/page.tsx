import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "lookbook · arjita" };

export default function LookbookPage() {
  return (
    <PlaceholderPage
      catNo="004"
      title="the lookbook"
      note="looks are being styled. soon you'll get to dress the archivist, and every outfit will open a different chapter — a project, a skill, a story."
    />
  );
}
