import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "projects · arjita" };

export default function ProjectsPage() {
  return (
    <PlaceholderPage
      catNo="001"
      title="projects"
      note="the project files are being written up — each one with what it does, why it exists, and what went wrong along the way. check back soon."
    />
  );
}
